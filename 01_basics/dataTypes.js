"use strict" // treat all js code as newer version of javascript

//alert(3+3); //it will give error as we are not running it in browser

//data types 
let name = "samarjit"//string
let age = 18 //number
let isloggedin = true //boolean
let state; //undefined
let temp = null // it is an empty value and its type is object
console.log(typeof temp)